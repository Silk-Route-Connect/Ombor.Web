VITE_OMBOR_API_BASE_URL=http://localhost:5062

# Mock layer (MSW). "true" — the Categories resource is mocked at the target v1
# contract (the real endpoint is stale at the page level: no product count, no
# reference-checked delete). See docs/mocking.md.
VITE_ENABLE_MOCKS=true
