VITE_OMBOR_API_BASE_URL=http://localhost:5062

# Mock layer (MSW). "true" while the backend does not yet satisfy the
# redesigned UI; only registered endpoints are mocked (hybrid mode).
VITE_ENABLE_MOCKS=true
