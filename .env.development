VITE_OMBOR_API_BASE_URL=http://localhost:5062

# Mock layer (MSW). Dormant ("false") — the backend covers every module the UI
# uses today. Flip to "true" only when a module needs a handler the backend
# can't satisfy (the worker starts but intercepts nothing until then).
VITE_ENABLE_MOCKS=false
