VITE_OMBOR_API_BASE_URL=http://localhost:5062

# Mock layer (MSW). "false" — the app runs against the real backend; no requests
# are intercepted. The src/mocks/** handlers are kept in the tree for reference
# but are never loaded. See docs/mocking.md.
VITE_ENABLE_MOCKS=false
