# CLAUDE.md — Ombor frontend

React 19 + TypeScript + MobX + MUI v7 client for **Ombor** — a warehouse and business-management system for small businesses in Uzbekistan replacing paper/Excel workflows. Core differentiator: a dispute-grade audit trail for debt, payments, and settlement. UI language is Russian (Uzbek backfill later); all amounts are UZS; all code, comments, and commits are English.

This file is the operating contract for every session in this repo. It points to canon documents instead of restating them — when this file and a canon doc conflict, canon wins; raise the conflict.

---

## Source-of-truth documents

Canon lives in `docs/canon/` (synced manually by Miraziz — treat as read-only; propose edits, never apply them).

| Document                         | When to read                                                                               | What to read                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| `docs/canon/business-rules.md`   | Any task touching domain behavior — transactions, payments, debts, stock, wallets, archive | The rule sections relevant to the task, plus **Domain model** and **Enum reference** for any entity you render or mutate |
| `docs/canon/product-brief.md`    | When a design/UX decision needs reasoning, or scope is ambiguous                           | **"Core design decisions and reasoning"** section only                                                                   |
| `docs/canon/mvp-plan.md`         | Start of any feature task                                                                  | Only the slice covering the current task                                                                                 |
| `docs/canon/tech-change-list.md` | When unsure whether a backend capability exists                                            | Relevant entries; assume anything "not started" must be mocked                                                           |
| `docs/openapi.json`              | Before integrating or mocking any endpoint                                                 | The exact current backend contract — routes, DTOs, params, error shapes. The authority on what exists today              |
| `docs/conventions.md`            | Writing or modifying any code                                                              | Whole doc once per session, then as reference                                                                            |
| `docs/mocking.md`                | Any task hitting an endpoint the backend lacks                                             | Whole doc                                                                                                                |
| `docs/backend-deltas.md`         | When flagging a backend gap, or starting a backend session                                 | The running frontend→backend handoff list — append new gaps with contract evidence; backend sessions work it off        |
| `docs/design-handoff.md`         | Implementing any screen from a Claude Design prototype                                     | Whole doc                                                                                                                |

Task-type quick map: **payments / debts / settlement UI** → business-rules §B + Domain model (Payment, Advance, worked examples). **Inventory / adjustments / transfers** → §D, §E. **Refunds** → §A. **Archive behavior in lists/pickers** → §G. **Any new page** → design-handoff.md + the mvp-plan slice.

---

## Commands

| Command                     | Purpose                                                                                                          |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `npm run dev`               | Dev server (Vite). **Never run it unprompted — assume it is already running.** If you need it and it isn't, ask. |
| `npm run build`             | Production build                                                                                                 |
| `npm run type-check`        | `tsc --noEmit`                                                                                                   |
| `npm run lint` / `lint:fix` | ESLint                                                                                                           |
| `npm run format`            | Prettier                                                                                                         |
| `npm run validate`          | type-check + lint. **Run before declaring any task complete.**                                                   |

No test suite in MVP — do not write tests unless explicitly asked.

---

## Repo map

```
src/
  components/<module>/   Per-module UI (Table, Form, Header, Autocomplete, …)
  components/shared/     Cross-module primitives (DataTable, FormDialog, NumericField, …)
  pages/                 One file per routed page
  stores/                MobX stores; RootStore composes, StoreContext provides
  services/api/          One Api class per module over BaseApi/http (axios)
  models/                TS types mirroring API contracts
  schemas/               Zod schemas for forms (via @hookform/resolvers)
  hooks/<module>/        Form hooks and module logic
  i18n/<locale>/         One JSON namespace per module
  routing/               Paths, guards (RequireAuth / GuestOnly)
  layouts/               AppLayout, Sidebar, Topbar
  theme.ts               Single MUI theme — the only styling source of truth
  utils/, helpers/, constants/
```

New code follows the existing module anatomy (see `docs/conventions.md`); do not invent parallel structures.

---

## Hard rules

1. **Immutable events get no edit/delete affordances.** Transactions, payments, payroll, stock adjustments, transfers: no edit buttons, no delete actions, no mutating forms. Corrections are separate counter-event flows (business-rules §A).
2. **No hardcoded UI strings.** Every string goes through an i18n key in the module's namespace. Fill `ru` values when adding keys; Uzbek locales are a later backfill pass — never block on them, never hardcode to avoid them.
3. **Side panes are deprecated.** The redesign uses full-page detail layouts. Never extend a `SidePane` component; when a module is being rewritten, its side pane is replaced and deleted.
4. **Currency machinery is frozen.** `CurrencyApi.ts` / `CurrencyStore.ts` stay in the tree but must not be used, extended, or fixed. The app is UZS-only (business-rules §H). All amounts render through `formatCurrency`.
5. **Never silently disable buttons.** Actions stay enabled; validation runs on submit and reports inline.
6. **Missing or stale backend = MSW mock of the target v1 contract, nothing else.** No hardcoded data in stores or components, no `setTimeout` fakes. Follow `docs/mocking.md`; every handler's shapes are written as the real future API contract.
7. **No scope additions.** Do not add features, fields, or flows beyond the current task and `mvp-plan.md`. Surface the idea; don't build it (business-rules rule 36).
8. **Computed balances are backend-computed.** Partner balance, wallet balance, "our money" are read from the API (or its mock) — never recomputed client-side from event lists.

## Git rules

- **Work on the branch the user currently has checked out.** Do not create branches in the repository. Local scratch branches are allowed only as a temporary working device and are never pushed; delete them before finishing.
- **Never switch, rename, rebase, or reset the user's branch** without explicit instruction.
- Commit under the repository's existing git identity only. **Never modify git config** (user.name / user.email).
- **No attribution trailers, ever** — no `Co-Authored-By`, no `Generated with Claude Code`, in commits or PR descriptions.
- Commit message format: `(<branch-name>) - <clear imperative summary>`, e.g. `(redesign) - migrate build tooling from CRA to Vite`. Small, scoped commits.
- **Decide the target branch before committing a feature.** When a module gets its own branch, agree the name (`redesign/<module>`, using the module/sidebar slug) and its base branch up front — don't author commits on one branch meaning to move them later. The `(<branch-name>)` prefix must match the branch the commits will actually live on.
- No push, no force operations, no history rewriting unless explicitly asked.

## Session discipline

- Orient before coding: read the docs the task-type map prescribes, state your plan in a few lines, then implement. For multi-page tasks, deliver one page/module at a time.
- If a needed fact is missing or a canon doc contradicts the task, **stop and ask** — never proceed silently on an assumption.
- Omitting or altering any designed element is never a unilateral call. If the prototype shows something the backend/canon can't support, or canon and prototype conflict, pause and ask the user mid-session — do not implement the deviation and report it in the summary. End-of-session "gap lists" are for discoveries that didn't change what you built, not for justifying changes you decided alone.
- If you discover a gap between code and canon that's outside the current task, report it at the end of the session; don't fix it silently.
- **Don't work around blockers that depend on the user or their environment — surface them and ask.** When something is failing because of the local setup (backend down, a port in use, CORS, credentials, env), stop and ask rather than building a workaround or weakening the app to get past it (e.g. never add an auth bypass to dodge a login that fails). State what you observed and what you think the cause is; the user can usually fix it fast.
- **Preview verification needs real auth on the right origin.** The dev preview must run on **`http://localhost:3000`** so the backend's CORS allowlist accepts it and login works; on any other port login fails at the browser (`net::ERR_FAILED`, not a 401). If `:3000` is taken by the user's own dev server, ask them to stop it (don't kill it yourself), then start the preview on `:3000` and log in with real credentials. Do not bypass the auth guard to verify.

---

## Verification & change discipline

- **Live verification before reporting done.** A green `tsc`/lint/build is NOT verification. Before reporting a task complete, run the app (or the relevant surface) and exercise the changed behavior against real or mock data — adoption and build-from-spec work silently drops prior behaviors and crashes on data shapes the build never sees. State what you verified live, not just that the build passed.
- **Migrating onto shared infra: flag behavior changes, get approval — don't absorb them.** When adopting a shared component/util changes any user-facing behavior (a lost interaction, a changed default, a dropped affordance), STOP and surface it as a decision. Do not rationalize it as "shared-component behavior" and move on. Get explicit approval before proceeding with the behavior change.
- **A missing field may be intentional, not a gap.** Before treating "the API doesn't return X" as work to do, check it against the domain/business rules (it may be deliberately out of scope — e.g. no persisted entity numbering in MVP) and against the contract sources. Confirm the field is genuinely absent _by design_ before proposing a backend change, a stub, or a fallback.

---

## Contract sources (frontend)

- When verifying whether a field or endpoint exists, check the contract, not memory: `openapi.json` (the generated contract in this repo) **and** `backend-contract.md`, plus the live API response. The types in `models/<module>.ts` mirror the contract — a field absent there and in `openapi.json` is genuinely unserved, not a binding oversight. This is the check that distinguishes "backend gap" from "intentional scope decision."

---

## Repo state (maintained — update when it changes)

- **Build tooling:** Vite + TS 5.x (migrated from CRA, June 2026). Husky/lint-staged removed.
- **Rewrite-as-we-go:** existing modules (Products, Partners, Transactions, Categories) are legacy side-pane style until rebuilt to the redesign. Structure and approach (stores/api/models/schemas) stay; components and layouts are replaced per module.
- **App shell:** Sidebar/Topbar/AppLayout rebuilt to the redesign (June 2026, locked pattern 10). All v1 routes exist in `routing/paths.ts`; unbuilt pages render the shared `PlaceholderPage`.
- **Theme / design tokens:** `theme.ts` is at full parity with the authoritative `tokens.css` + the **DSN-1 foundational-components reference** (spec-lock, the design bundle's "Базовые компоненты" page). It is the **single styling source — no inline hex anywhere**; components reference its exports. Exports: `designTokens` (raw token ramp — now incl. `gray0`/`gray800`/`gray900`, `textSecondary`, `saffron100`, `warningBorder`, `focusRing`), `numericSx` (tabular **lining** figures), `radius` (`xs/sm/md/lg/pill`), `typeScale` (display + numeric `numHero/numStrong/numTable`), and **`chipTokens`** — the locked chip colour-semantics: **transaction type** Sale=teal-soft / Supply=saffron-soft / SaleRefund=teal-outline / SupplyRefund=saffron-outline (brand hues — green/red reserved for money), **payment status** Open=info / PartiallyPaid=warning / Overdue=error / Closed=success, **direction** Income=success / Expense=error. `palette.text.secondary` now maps to `--text-secondary` (gray-600, the data-bearing secondary colour for SKU/dates/№); **gray-400 is decoration-only** (placeholders/disabled), never data text. Typography gained `h3`/`caption`/`overline` to match the sheet's scale; Onest is the family incl. numerics. **Chips wired (FE-INFRA-1 follow-up, June 2026):** the entity chips that map cleanly to `chipTokens` now consume it (no inline `alpha`/stale mapping) — **transaction type** `TransactionKindChip` + `TransactionTypeBadge` (Sale=teal / Supply=saffron / refunds outlined, **+ Sale/Supply/refund icons**, fixing the stale Supply=blue), **transaction status** `TransactionStatusChip` (now keys off the served `TransactionStatus` enum Open/PartiallyPaid/Overdue/Closed — the old `paid/partial/unpaid` `PaymentStatus` was display drift; `statusOf()` added, mock derives `status`; `paymentStatus`/`payStatusOf` **kept** only for the list filter + CSV, whose vocab is the transaction module's own pass), **money direction** `PaymentDirectionBadge` (→income/expense) + stock-adjustment `DirectionChip` (Increase→income / Decrease→expense, colour reuse). `PartnerTypeChip` renders `Both` as «Клиент + Поставщик». **Still on the stale/own mapping (no clean chipTokens key — left for module passes):** `OrderStatusChip` (7-state machine; already token-clean) and the module-local Sale/Supply chips `ProductTypeChip` / `MovementKindChip` / `TemplateTypeChip`. NB: «Overdue» = `--error` (tokens.css has a single red; the sheet's "усиленный красный" is emphasis, not a separate token).
- **Warehouses (Склады):** rebuilt to the redesign (list + full-page detail with Остатки/Движения tabs, create/edit modal, opening-stock modal, archive/restore). Mocked at `/api/warehouses` (the backend resource is the stale `/api/inventories`); stock + movements are derived from the Products mock so the two reconcile.
- **Stock Adjustments (Корректировки):** rebuilt to the redesign (immutable list with expand-row detail, warehouse/direction/search filters, create modal with the direction toggle + reason-by-direction + hard-blocked over-stock validation). Mocked at `/api/stock-adjustments` (no backend endpoint exists); references real products/warehouses, does not mutate their stock (known mock limitation).
- **Transfers (Перемещения):** rebuilt to the redesign (immutable list via shared DataTable with warehouse filter, read-only detail modal with the from→to route + lines, multi-line create modal with route picker (from≠to), per-line availability + hard-blocked over-stock). Mocked at `/api/transfers` (backend DTO lacks author/unit and carries an unused status); references real products/warehouses, does not mutate their stock (known mock limitation).
- **Partners (Партнёры):** rebuilt to the redesign (list with summary strip + search/type/archive filters + CSV; full-page routed detail `/partners/:id` with balance card + the dispute-grade running-balance ledger and Журнал/Транзакции/Платежи tabs; create/edit modal with opening balance editable-at-create/locked-on-edit; archive/restore; **delete reference-gated** — deletable only when unreferenced, else a «cannot delete» warning). Mocked at `/api/partners` incl. a new `GET /api/partners/{id}/ledger` (server-computed balance, opening-balance event, archive, and ledger are all "not started" on the backend); the ledger is self-contained (does not cross-reference the Products/Transactions mocks). No system «Розничный покупатель» — partners are uniform (per product owner). Legacy `SelectedPartnerStore` + `PartnerAutocomplete`/`PartnerLink`/`PartnerBalanceTooltip` kept for the still-legacy transaction flow.
- **Sales / Supplies (Продажи / Поставки):** rebuilt to the redesign as one `direction`-parameterized module (`"Sale" | "Supply"`). List = unified immutable feed of the direction's transactions + refunds (search, payment-status segmented, functional date-range dropdown, CSV, empty states; no row menu — row → detail; refund rows negative + «Возврат к #N»). Routed full-page detail (`/sales/:id`, `/supplies/:id`) renders sale/supply or refund detail by type (positions + totals, refund-history, note/attachments, audit, partner-mini, financial card, payments; refund adds reference banner + mandatory reason, no refund action). Refund-create modal from the detail ⋮ (per-line cumulative cap + mandatory reason, business-rules §A). Mocked at `/api/transactions` (GET list, GET /{id}, **POST /{id}/refund**); totals computed from line items so screens reconcile; **New Sale + New Supply both create via `POST /api/transactions` as `multipart/form-data` — a single `payload` part (the `direction`-discriminated `CreateTransactionEntryRequest` JSON, minus files) + zero or more `attachments` file parts (mocked; the handler reads the `payload` part + `attachments` files). The real backend must persist the binaries to blob storage; the mock keeps attachment metadata only.** Refund doesn't mutate Products stock (known mock limitation). Legacy `TransactionPage` side-pane replaced; the **legacy create-flow was deleted** (June 2026 cleanup) — `CreateTransactionPage`, `components/transaction/Form/**` (incl. `DebtPayment/`), `useCreateTransactionForm`/`DetailsState`/`PaymentsState`/`PaymentSummary` hooks, `SelectedPartnerStore`, the legacy `TemplateAutocomplete`/`PartnerBalanceTooltip`/`PartnerLink`, and the now-orphaned store/api/model surface (`TransactionStore.create`/`createPayment`, `TransactionApi.create`/`createPayment`/`getFormData`/`getPaymentFormData`/`getOpenTransactions`, `PartnerApi.getPayments`, `TemplateStore` legacy `saleTemplates`/`supplyTemplates`/`filteredTemplates`/`selectedPartner`/`setSelectedPartner`, `PartnerStore.selectedPartner`/`setSelectedPartner`, `CreateTransactionRequest`/`CreateTransactionLine`/`DebtPayment`/`TransactionPaymentRecord`/`CreateTransactionPaymentRequest`/`DashboardMetrics` types). `PartnerAutocomplete` is **kept** (live Order + Template modals use it); legacy `models/payment.ts` payment types kept (Payroll/Payments). **Transactions bug-fix pass (TXN-1…7 + extras, July 2026, `redesign/bug-fixes`):** list moved to the locked conventions — columns **№, Дата, Тип, Партнёр, Позиций, Сумма, Статус** (№ = shared `CopyableNumberCell`, extracted from Orders' `OrderNumberCell`; partner = `PartnerLink`), every column sortable (date-desc default; the date sortValue carries a **+0.5 ms refund offset** because DataTable's desc = stable asc + reverse, which would flip the store's refund-above-original tie; № falls back to numeric `tx.id` to avoid lexicographic order), canonical 10/25/50 pager; the **status filter + CSV now use the served `TransactionStatus` enum** (Все/Не оплачено/Частично/Просрочено/Оплачено via `statusShort.*`) — derived `paymentStatus`/`payStatusOf`/`PaymentStatus` deleted; **`TransactionApi.create` maps its response through `toRecord`** (a raw prepend used to carry a string `date` — crashed the new default sort); refund − amounts **kept** (D12: intentional). Detail adopted the shared `DetailPageHeader` under the **name-only rule (owner, July 2026): the header carries ONLY «№N»** — no chips, no meta line (Скачать as primaryAction, refund create in the kebab; refund variant kebab-less). Everything else lives in body sections: the `AuditCard` («Информация», top of the right column: **partner row (`PartnerLink`, label Клиент/Поставщик) → created → warehouse (guarded)**, then financial card (status row) → payments); `PartnerMiniCard` deleted (owner: a lone partner card was redundant — folded into Информация); **no partner balance anywhere on the detail (owner: current balance on a historical transaction misleads)**. The **Orders detail header was aligned to the same rule** (its status/source chips + created·customer meta removed — all duplicated by the stepper/sidebar/history; `order.detail.createdOn` key deleted). `TransactionTypeBadge` `large` and `TransactionStatusChip` `full` props removed (dead) along with the `transaction.status.*` long-label keys. **`MetaDot`** (shared 4px gray-400 dot, `shared/Detail/`) replaced the invisible 3px gray-300 separators app-wide (transaction cards, RefundModal, Order header meta). **Backend deltas recorded in `docs/backend-deltas.md`** (no Overdue in the served enum, list DTO lacks `number`, `originalTransactionNumber` never served).
- **New Sale + New Supply / POS (Новая продажа / Новая поставка):** rebuilt to the redesign at `/sales/new` + `/supplies/new` as **one `direction`-parameterized component** `components/transaction/Create/NewTransactionEntry.tsx` (thin `NewSalePage`/`NewSupplyPage` wrappers); state + money math in `hooks/transactions/useTransactionEntry.ts`; supporting components under `components/transaction/Create/`. Full-page POS: breadcrumb + back, **partner/supplier picker (required — no system walk-in partner; canon rules 39–40 superseded per product owner, see design-handoff pattern 9)** showing served balance as colour + label, warehouse picker (defaults to first), product-search cart (multi-add), rich line (qty stepper · editable price · line discount **% or per-line fixed** + toggle · live total + discount amount), **bulk «применить скидку ко всем»** (overwrites lines, rule 38), notes + attachments. Right summary card: partner balance + projected «баланс после …», Подытог→Скидка→Итого, always-on payment breakdown (single-wallet tender; on overpay: optional debt settlement + **Сдача/Аванс** toggle, advance only at zero remaining debt — rule 40), «Вся сумма», immutability note, submit. **Reuses `PaymentSettlementModal`** (outstanding via `GET /api/payments/outstanding`; direction Income/Expense). Dialogs: no-payment confirm + unsaved-changes guard. **Templates load + save** of the current direction. **Direction deltas (per the design):** Sale prices from `salePrice` + shows per-line stock + **hard-blocks over-stock** (rule 20); Supply prices from `supplyPrice`, shows «Цена поставки», **no stock validation** (a supply adds stock); balance projection flips sign (Sale ↑ receivable, Supply ↑ payable / «Мы должны»); all copy keyed by direction. **Line fixed-discount is per-line currency (canon rule 37 + the detail/refund math), NOT the prototype's per-unit fixed** (flagged deviation); qty **clamped ≥1** (prototype's zero-qty error dropped). **POS keyboard/UX:** search **autofocuses**; picking a product **removes it from the dropdown** + **focuses/selects its qty**; **↑/↓** steps qty; **Enter** (qty/price/discount) → search; **⌘/Ctrl+Enter** submit; **Esc** leave (guarded); **Alt+P / Alt+W** focus pickers; discoverable **`KeyboardHints`** legend. Dropdowns use a shared design-system popper (`Create/dropdownSx.ts`, teal primary-soft hover — needs `&.Mui-focusVisible`+`!important`); the product-search input has a **white background** (sits on the canvas, not a card). Self-contained mock (`addTransactionEntry`): the created sale/supply joins the feed + reconciles on its detail, but stock / partner balance / wallet balance are **not** mutated, and the settlement/advance disposition is illustrative (known limitation, like refunds).
- **Templates (Шаблоны):** rebuilt to the redesign (list with search (name/partner) + type segmented filter, expand-row detail showing the line items + footer total, ⋮ row menu with Edit/Delete; create/edit modal with name + sale/supply type toggle that re-prices lines, all-partners autocomplete, product-search cart with qty stepper + editable price, discard-changes confirm; delete confirm). A template is an **editable** basket — not an immutable event — so edit/delete are allowed (mvp-plan §12). Mocked at `/api/templates` (the live `TemplateDto` exists but lacks the `lastUsedAt` «Использован» date and per-item SKU/unit the redesign shows); lines reference real products and partners, totals computed from line items. The prototype's «Использовать» (load into a transaction) is now **wired in the rebuilt New Sale** (load + save-as-template, see the New Sale note); the legacy New Supply create flow still loads templates via its `TemplateAutocomplete` (kept). `TemplateStore` keeps its legacy `setSelectedPartner`/`saleTemplates`/`supplyTemplates` surface for that autocomplete, decoupled from the list's search/type. **Templates bug-fix pass (TPL-1…6, July 2026, `redesign/bug-fixes`):** the bespoke list table was replaced by the shared `ExpandableDataTable` (warm header band, every column sortable with name-asc default, canonical 10/25/50 pager — TPL-1/2/4; expand panel = the line-items table with `ProductLink`s, partner cell = `PartnerLink`); `TemplateTypeChip` **deleted** — rows use the shared `TransactionTypeBadge` (TPL-3); `TemplateActionsMenu` **deleted** — the shared `ActionMenu` (edit + danger delete) took over, and the shared component gained a **localized aria-label** (`common.actions`, was hardcoded "actions") while `ExpandableDataTable` gained the **localized pager labels** it was missing (both pre-existing shared-infra gaps surfaced by this migration). Modal: rows restructured to (Название / Партнёр) + Тип below (TPL-5), the type toggle compacted to the 38px md control height with chipTokens sale/supply hues + standard icons (TPL-6, closing the last stale blue/orange Sale/Supply mapping), name input 46→38. **Backend deltas recorded** (`docs/backend-deltas.md` §6): `TemplateDto` lacks `lastUsedAt` («Использован» always «—»), `TemplateItemDto` lacks `sku`/`measurement` (empty expand-row cells vs the real backend) and **`discountType` — the FE computes line totals treating `discount` as a percent while live data carries fixed amounts, producing the absurd negative totals visible on the deployed list** (pre-existing math, deliberately not changed client-side until the contract defines the semantics).
- **Orders (Заказы):** rebuilt to the redesign — the one **mutable** transaction (a customer's requested goods before stock/money move). List (`/orders`): status tabs with live counts + search + functional date-range + CSV; routed full-page detail (`/orders/:id`) with status stepper, terminal banner (cancelled/rejected/returned), positions, delivery info, status-history timeline, partner-mini (balance as color + natural-language label), financial card, promote-to-sale hint, and a ⋮ menu whose actions depend on status. State machine wired to `process`/`ship`/`deliver`/`cancel`/`reject`/`return`. Edit modal (client, source, product cart with qty/price/discount, address, requested delivery date, note; editable pre-delivery). Each order carries a **requested delivery date + time** (`deliveryDate`/`deliveryTime`, added to the design after the first pass): a **list column** with overdue (red, past + pre-delivery) / upcoming / done states, a detail delivery-card row (with «Просрочена» badge), and **required** date + optional time in the edit modal. **Warehouse is chosen at the delivery-confirmation dialog** (prototype flow) with a per-line stock-check that hard-blocks shortfalls (rule 20) — this overrode the earlier "warehouse at creation" wording in business-rules Domain model + mvp-plan §11, both updated. Mocked at `/api/orders` incl. the transition endpoints (the live `OrderDto` lacks status history, warehouse, the promoted-sale link, and per-line SKU/unit); lines reference real products, customers reference real partners. Promotion is **self-contained**: delivering marks the order Delivered with a sale reference + history but does NOT write the Sale into the Transactions mock or mutate stock (known limitation, like refunds/transfers) — the «Продажа #N» link shows an info toast. The «Новый заказ» create flow is the separate full-page New Order screen (`/orders/new`, below).
- **Wallets (Касса):** rebuilt to the redesign — answers "how much cash do I have, and where?". List (`/wallets`): summary strip (Общий баланс / Наши средства / Авансы партнёров, spanning **all** wallets incl. archived per rule 31) + search + archive toggle + CSV; table (касса with type-tinted icon, type badge, balance, partner advances, our money) with a ⋮ row menu (edit / archive·restore — never delete). Routed full-page detail (`/wallets/:id`): breadcrumb/back header with type + archive badges and «Начальный остаток · создана» meta, ⋮ menu (edit / archive) or primary «Восстановить», archive banner, three stat cards (Баланс / Наши средства / Авансы), and two tabs — **Операции** (immutable money-movement ledger: date · payment# / Перевод chip · type · direction pill (Приход green / Расход red, **no +/− signs**, locked pattern 4) · party · amount · running balance; search + direction segmented filter; payment rows toast, transfer rows open the transfer detail) and **Переводы** (inter-wallet transfers with «Новый перевод»). Modals: create/edit (name + type segmented + opening balance on create; **type + opening locked on edit**, rule 16), inter-wallet transfer (icon wallet picker, from≠to, live route preview, available-balance hint + «Перевести всё», hard-blocked over-balance, immutable warning), read-only transfer detail. All balances / advances / our-money are **server-computed and served** (rule 12) — never recomputed client-side. Mocked at `/api/wallets` incl. `/operations`, `/transfers`, archive/restore, and `POST /api/wallets/transfers` (the **whole resource is "not started" on the backend**). **Self-contained** (like the Partners ledger): the operations ledger + transfers are seeded here and do **not** cross-reference the legacy Payments mock — payment numbers («P-520») are illustrative and a payment-row click only toasts; a created transfer adjusts both wallets' served balances + appends an operation to each side. Operations' `balanceAfter` running balances are illustrative (not reconciled to the opening balance — a known mock limitation). The prototype's per-ledger **period date filter and static pager are omitted** (locked pattern 12). **Wallets bug-fix pass (WAL-1…18, July 2026, `redesign/bug-fixes`):** the **list page** moved onto the shared `DataTable` (warm band, sortable columns, name-asc default, 10/25/50 pager) and its archive control from the old toggle-switch to the shared **`SegmentedControl` «Активные | Архив (N)»** with **show-only** semantics (`WalletStore.filteredWallets` fixed from add-archived to `showArchived ? isArchived : !isArchived`, matching Partners/Warehouses — WAL-1/2/4/5 for the list; the list ⋮ was already the shared `ActionMenu`). Both hand-rolled **detail** ledger tables (Операции + Переводы) → shared `DataTable` (warm band, sortable columns, canonical 10/25/50 pager — WAL-3/4/5). **Операции** columns Дата/Платёж/Тип/Направление/Партнёр-получатель/Сумма/Баланс после: the **Платёж** cell links payment rows to `/payments/:id` (served `paymentId`) and shows «—» for non-payment rows (WAL-9 de-dup — the old double-«Перевод» gone); direction is the **new shared `DirectionBadge`** (chipTokens income/expense + ↓/↑; `PaymentDirectionBadge` repointed to it — WAL-10/G5); «Баланс после» now bold `text.primary` (WAL-11); the «Сумма, UZS» header → «Сумма» (WAL-12). The **«Новый перевод» button moved to the detail header** primaryAction slot (WAL-13, hidden when archived). **Переводы** tab also migrated to `DataTable`. Transfer modal: source→dest pickers on one row with a chevron (WAL-17), «Перевести всё» beside the amount input (WAL-18), balance dropped from the picker's selected value — kept in menu items + the «Доступно» hint (WAL-16), notes multiline (WAL-15), «изменить или удалить нельзя» clause dropped + submit «Провести перевод»→«Перевести» (WAL-14). **Already-correct (no work):** list ⋮ = shared `ActionMenu` (WAL-1), archive shows-only (WAL-2), date uses the shared format (WAL-8). **WAL-7 (clickable party) is a backend delta** — `WalletOperationDto.party` is a name string with no `partnerId` (recorded in `docs/backend-deltas.md` §7); party stays plain text. **Pre-existing latent bug flagged (not fixed, out of scope):** the transfer modal's over-balance guard (`amount > available`) blocks ALL transfers from a wallet with a negative served balance (even amount 0), so an overdrawn wallet can't be a source — needs `Math.max(available, 0)` when negative balances are possible. NB: `/api/wallets` + sub-resources are **real backend endpoints** now (the "not started" note above is stale — verified in `openapi.json`).
- **Payments (Платежи):** rebuilt to the redesign — the standalone payment flow (outside the New Sale/Supply inline flow). Five types: **Оплата** (Transaction settlement) · **Депозит** (advance in) · **Вывод** (advance returned) · **Зарплата** (payroll) · **Общий** (business income/expense). Payments are **immutable** (rule 1) — no edit/delete, no row actions; reverse-payment is out of MVP (omitted per the final prototype). List (`/payments`): summary stat cards (Приход / Расход / count over the filtered view) + search + **type dropdown** + **wallet dropdown** + CSV; table (№ · date · type badge (5 colors) · direction badge (↓Приход green / ↑Расход red, **no +/− signs**) · partner/employee · wallet · amount). Routed full-page detail (`/payments/:id`): minimal header («P-520 · Платёж»), immutability strip, left column = **Касса** source line + **Распределение** allocation table (TransactionSettlement / AdvanceCredit / ChangeReturn memo) — with payroll-grid and general-description variants — right column = **Информация** metadata card; the partner row links to the partner detail. Create (large modal): type segmented first step → per-type clean fields (partner picker with served balance + advance, direction auto-derived/«Оба»-or-General toggle per rule 14, wallet, amount); **Оплата** shows a debts info banner when the partner has open transactions and opens the **standalone settlement modal** (lists outstanding, FIFO auto-allocate, manual per-row, excess → advance — business-rules §B); **Вывод** hard-blocks over-advance. **Payroll allows any number of payments per employee+month** (canon — the prototype's one-per-month block was dropped as a canon conflict). Sources balance settling allocations (rule 8); all partner balances / advances / wallet balances are **server-computed and served** (rule 12). Mocked at `/api/payments` incl. `/form-data`, `/outstanding`, and `POST` (the live backend DTO is the **removed-enum** model — method/currency/exchangeRate — stale for the source/allocation model). **Self-contained** (like the Partners ledger): reference data + outstanding are seeded here and do **not** cross-reference the other mocks; a created payment adjusts the served figures it touches (outstanding paid, partner advance/balance, wallet balance). The **legacy** `models/payment.ts` types (method/currency, allocation types Sale/Supply/…) are **kept** — still used by Payroll / Transactions / the New Sale debt-payment flow — and the redesigned `PaymentRecord` types are added alongside; the legacy `PaymentSidePane` + side-pane tables were deleted, `PaymentStore`/`PaymentApi` rewritten. The prototype's period date filter + static pager are omitted (locked pattern 12).
- **Debts (Долги):** rebuilt to the redesign — a **read-only, aggregated** view over unpaid / partially-paid transactions (NOT a new entity; no create — debts are produced by transactions). List (`/debts`): four summary cards (Нам должны green / Мы должны red / Просрочено warning / Чистая позиция) — label + amount + transaction count, **no sparklines**; the first three are clickable and jump to the transactions tab with a preset filter (Просрочено → only-overdue, sorted by age). Two tabs with a colour legend: **По партнёрам** (grouped: partner · type chip · txn count · oldest debt date + overdue/«в сроке» chip · signed total — sorted by absolute debt desc) and **По транзакциям** (flat: document # + date · type chip · partner · total · paid-with-progress-bar · remaining · age + «просрочка N дн» chip). Shared filters: search (leftmost) + age buckets (Все/0–7/8–30/31–60/60+); the transactions tab adds a direction segmented (Все/Нам должны/Мы должны). **No +/− signs** — colour only (locked pattern 4). CSV export. All figures (remaining, age, overdue) are **server-computed and served** (rule 12); summary + groups are aggregated client-side from the served list. **`/api/debts` is a REAL backend endpoint now** (the served `DebtDto` carries `transactionId`/`number`/`direction`/`transactionType`/partner fields/`total`/`paid`/`remaining`/`ageDays`/`overdueDays`); the model mirrors it field-for-field. Age/overdue are server-computed. **Debts bug-fix pass (DBT-1…4, July 2026, `redesign/bug-fixes`):** both hand-rolled tables → shared `DataTable` (warm band, every column sortable, canonical 10/25/50 pager — DBT-3/4); page **subtitle removed** (DBT-2); the transactions tab's **sort dropdown was dropped** (owner — column headers own ad-hoc sorting; the dashboard/summary-card presets seed the table via `DebtStore.txPresetSort` + a `txPresetNonce` that re-seeds even on a same-preset re-click, keyed on `DebtPage`); the local green/orange chips replaced by the shared **`TransactionTypeBadge`** (transactions tab, refund-aware sortValue) and **`PartnerTypeChip`** (partners tab, Receivable→Customer/Payable→Supplier); document cell + CSV use `formatEntityId` («№…»); partner cells are real **`PartnerLink`**s (scoped inline `stop` so the row click still opens the transaction detail). DBT-1 (partner deep-link `?tab=transactions&status=open`) was **already wired** by the Partners pass; the transaction-row click already opens the real sale/supply detail (no more toast). Dead i18n removed (`debt.subtitle`/`debt.sort.*`/`debt.txType.*`/`debt.partnerType.*`/`debt.openTransaction`).
- **Employees (Сотрудники):** rebuilt to the redesign from the legacy side-pane module — **real backend** (`/api/employees`, `/api/employees/{id}/payrolls`), not mocked. List (`/employees`): search (name/position) + status segmented (Все/Активный/В отпуске/Уволен) + CSV; table (сотрудник avatar+name · должность · зарплата · status badge · дата найма) with a ⋮ row menu (Выплатить (active only) / Редактировать / **Уволить·Восстановить** — a status change, never a hard delete: employees carry a Terminated status). Routed full-page detail (`/employees/:id`, **replaces the deleted side pane**): identity header (avatar + name + status badge + «должность · телефон · с дата»), primary «Выплатить» (or «Восстановить» when terminated) + ⋮ menu, three stat cards (Зарплата / Выплачено за <месяц> / Выплат всего), and the **Выплаты** payroll-history table (date · type · derived period · amount · wallet) with a Неделя/Месяц/Весь период preset filter. Reuses the working centered **create/edit modal** (`EmployeeFormModal`) and **payroll modal** (`PayrollFormModal`, opened locked-to-employee). Terminate/restore go through `EmployeeStore` (status update + confirm dialogs); the legacy hard-delete is no longer exposed in the UI. **Payroll allows any number of payments per employee+month** (canon — no one-per-month block, matching the Payments decision); the backend payroll has no period field, so the period column is derived from the payment date and all rows show a «Зарплата» tag (the prototype's salary/advance subtype isn't in the data). Deleted the legacy `EmployeeSidePane` + tabs, the old `EmployeeTable`/`employeeTableConfig`/`EmployeeStatusSelect`/`EmployeeStatusChip`. The separate `/payrolls` `PayrollPage` is **vestigial** (not in the sidebar — Кадры has only Сотрудники; payroll lives inside the employee detail). **Employees audit-fix pass (EMP-1/3/4/6, July 2026, `redesign/bug-fixes` — the module is NOT in `bug-fixes-plan.md`; audited against the standards the other passes set):** the **list table** and the **detail payroll-history table** both migrated from hand-rolled `tableStyles.ts` tables to the shared `DataTable` (warm band, sortable columns, 10/25/50 pager; list name-asc, payroll date-desc). The **detail header** moved to the shared `DetailPageHeader` under the **name-only rule** (title = name; primaryAction = Выплатить/Восстановить; kebab = edit + terminate·restore) — the avatar + status badge + должность/телефон/hire-date that were in the header now live in a new **identity card** at the top of the body (exactly how Partners moved identity into its balance card). `EmployeeStatusBadge` is now **token-clean** (removed the `#17835A` hex, added borders): **Active = success green / OnVacation = warning orange / Terminated = neutral stone-gray** (owner) — plus a defensive fallback because the served `EmployeeDto.status` is a nullable string, not the enum (`STATUS_TINT[status] ?? neutral` + raw-value label so a null/unknown status can't white-screen the per-row badge). Already conformant (no work): the status filter was already a shared `SegmentedControl`, the row ⋮ already the shared `ActionMenu`, the form/payroll modals already use `MoneyField`. `EmployeeStore.setSort/sortField/applySort` are now vestigial (the DataTable sorts client-side). NB: the local test backend serves **zero payroll records**, so the payroll `DataTable` was only exercised at its empty state live.
- **Dashboard (Главное):** rebuilt to the redesign — the read-only morning briefing at the landing route (`/`, the dashboard index; was `PlaceholderPage`). No data entry; only navigation. Header: title + «<business> · <period sub>», a functional **period selector** (Сегодня/Неделя/Месяц, default Месяц — drives the revenue KPI + both charts). **Four clickable KPI cards** (Выручка → /sales · Нам должны → /debts receivable · Мы должны → /debts payable · Просрочено → /debts overdue): hero tabular value, period-delta badge (±), sparkline; receivable/payable are **colour-only, no +/− signs** (locked pattern 4 — the prototype's signed values were dropped) — the KPI→debts clicks call `debtStore.applyCard` before navigating. **Two charts** (recharts, ~70/30 left/right grid): «Динамика продаж и поставок» (smooth area-filled lines default ↔ bars, teal sales / saffron supplies) and «Платежи» (diverging bars green-in/red-out default ↔ net line, with a **касса filter** narrowing to one wallet). **Aging panel** (Дебиторка по срокам — stacked proportion bar + 4 escalating buckets + 31+ overdue banner + «Всего к получению» footer; not clickable), **Топ должников** (5 receivable partners → real `/partners/:id`, «Все партнёры» → /partners), and **Последние транзакции** preview (8 rows, row click toasts). Kept a **read-only preview by design** (canon: "last 7–10, no pagination, a preview not a full list") — the full paginated/filterable lists are the dedicated pages, so the header carries **three explicit links** «Все продажи» → /sales · «Все поставки» → /supplies · «Все заказы» → /orders (the single «Все»→/sales was misleading since the preview shows both directions; making it a full all-types+orders feed was declined to avoid duplicating those pages — Orders aren't immutable transactions and carry status, not payment status). Data-driven **empty/welcome** state (heading + 3 first-step cards) when the snapshot has no activity — the prototype's «Пример: С данными / Новый бизнес» demo toggle is **omitted** (a debug device, not app UI, per design-handoff). The «Свой период» custom date-range and the per-chart PNG / page «Экспорт» PDF affordances are **omitted** (custom range = locked pattern 12; export deferred to Reports v2 per the user). Mocked at `/api/dashboard?period=` (**no backend dashboard endpoint** — a served read model). **Debt-derived figures (receivable/payable totals + counts, aging, top debtors) are computed from the SAME seed as `/api/debts` (`listDebts()`), so the two screens reconcile** (mvp-plan §2 Done criterion); the time series + revenue are illustrative deterministic pseudo-random, and recent transactions are a self-contained seeded preview (row click toasts). NB: the dashboard's «Просрочено» is **31+-days-aged** receivables (the aging banner's own definition), distinct from the `/debts` past-due-date overdue — so against the current date it can read 0 while /debts shows due-date overdue (a faithful prototype divergence).
- **Settings (Настройки):** rebuilt to the redesign (mvp-plan §18) — a single page at `/settings` (was `PlaceholderPage`) with a sticky left section-nav (scroll-spy over the `<main>` scroll container) and four stacked cards: **Организация** (editable name/address/phone/email + logo upload → data URL), **Язык** (per-user interface language radio cards — applies **immediately** via i18n like the Topbar globe, NOT staged in the save bar; options come from a new shared `i18n/languages.ts` used by both Topbar and Settings), **Валюта** (read-only locked UZS + info note, rule 33), **Пользователи** (list with «Администратор» chips + no-roles footnote; **invite** modal (email/phone, on-submit inline validation, send never disabled); **deactivate/reactivate — never delete** (rule 41), deactivate behind a confirm dialog, self-account guarded with a toast, deactivated rows greyed + «Деактивирован» chip + restore). Bottom **save bar** governs the Organization form only — buttons stay **enabled** (hard rule 5; prototype disabled-when-clean was overridden), status note shows dirty/clean. The prototype's «Данные»/CSV-export section is absent (cut from MVP, mvp-plan Deferred). Mocked at `/api/settings/organization` (GET/PUT) + `/api/settings/users` (GET, `/invite`, `/{id}/deactivate|reactivate`) — **no backend exists** (only `/api/auth/*`); self-contained mutable mock. Language uz-Cyrl is **gated** out of the language list until its resources land (matches the existing Topbar decision; prototype showed 3, repo offers ru + uz).
- **New Order (Новый заказ):** rebuilt to the redesign — the full-page order-create POS at `/orders/new` (was `PlaceholderPage`; `NewOrderPage` → `components/order/Create/NewOrder.tsx`). Mirrors the New Sale POS but for a **pending intent** — NO payment, NO wallet, NO immutability warning (instead an info note «Заказ можно изменить или отменить до доставки. Деньги и склад не затрагиваются.»). Left column: a selectors card (**Клиент** required — reuses the shared `PartnerPicker` direction="Sale" over `partnerStore.customers`, balance as colour+label; **Склад** required, defaults to first; **Источник** Нет/Telegram/OmborWeb via `OrderSourcePicker`) + a second row (**Дата доставки** required · **Время доставки** optional), then the shared `ProductSearchBar` (direction="Sale", sale price + per-warehouse stock) and the cart of `OrderLineRow`s (qty stepper · editable price · line discount % or fixed). Right summary card: customer balance card, summary rows (Клиент/Склад/Доставка/Подытог/Скидка/Итого), address + note inputs, the editable-note, «Создать заказ». **Stock is shown per line as guidance only — over-stock is flagged «· больше остатка (не блокирует)» but NEVER blocks creation** (the real stock check is at delivery, rule 20; contrast the Sale cart which hard-blocks). **Warehouse IS collected at creation** (per product-owner decision, overriding the «warehouse only at delivery» wording the shipped Orders module took): it is stored as the order's **intended** warehouse (pre-fills the delivery dialog) but stock isn't reserved; the detail's «Склад списания» row is **gated to Delivered/Returned** so a pending order's intended warehouse doesn't mislabel as written-off. **Edit/create reconciled:** the **Edit-order modal** (`OrderFormModal`) now has the (optional) intended-warehouse picker too — a `TextField select` (Client | Warehouse | Source row) with a «Не выбран» empty option (`displayEmpty`); `UpdateOrderRequest.warehouseId?: number | null` threads through `OrderApi`/`OrderStore`/mock `updateOrder`, which distinguishes **undefined** (omitted → keep existing) from **null** (explicit clear) from a value (set). The «Склад списания» detail row stays gated to Delivered/Returned. Posts `CreateOrderRequest` to **`POST /api/orders`** (mocked, returns 201; new id = max-seed+1, status Pending, history seeded; self-contained — does NOT mutate product stock or partner balance, like the rest of the Orders mock). Unsaved-changes guard + on-submit inline validation (client/warehouse/date/≥1 line). Reuses `OrderStore.create`/`OrderApi.create`. No templates/bulk-discount/keyboard-loop (the order prototype is deliberately simpler than New Sale).
- **Auth (Вход / Регистрация / Восстановление пароля):** rebuilt to the redesign — the standalone two-panel auth surface (teal `BrandPanel` + form panel + grid-bg, `layouts/AuthLayout` rebuilt as the shell; shared field primitives in `components/auth/AuthFields.tsx`, chrome in `AuthChrome.tsx`, language footer in `AuthLangFooter.tsx` wired to the shared `UI_LANGUAGES` + i18n). **Route-based under `GuestOnly`** (kept the existing routing/guard model rather than the prototype's single-page swap): `/login`, `/register`, **new `/reset-password`**. **Login** (`LoginPage`) — phone (+998, 9 national digits) + password + «Забыли пароль?» → reset; real backend. **Register** (`RegisterPage`) — `form → otp → welcome` steps in-page: lean form (company + **Имя + Фамилия** + phone + password×2 + terms; **email/Telegram dropped** per product-owner, though the backend still accepts them) → **4-digit** OTP (the design's 6 was overridden — the real `/verification` is 4 digits) → the «Добро пожаловать» welcome screen with 3 onboarding steps → «Начать работу». Two-phase auth so the welcome shows before entering: `authStore.verifyOtp` returns the token WITHOUT entering, `enterWithTokens` commits on «Начать работу» (avoids `GuestOnly` bouncing the welcome). **Reset password** (`ResetPasswordPage`) — `phone → code → newpass → success` steps; **MOCKED at the target v1 contract** (`mocks/handlers/auth.ts`: `POST /api/auth/forgot-password`, `/verify-reset-code`, `/reset-password`) because the backend has **no reset endpoint** (only login/register/verification/refresh/logout); the mocked demo code is **`1234`** (per product-owner it is NOT surfaced in the UI — documented only here + in the handler; no demo hints anywhere, since login/register hit the real backend). Validation is manual + inline-on-submit (`utils/authValidation.ts` — phone format/mask + field-error helpers returning i18n keys), no zod/RHF for auth. The legacy MUI `AuthLayout`/`RegistrationForm`/`OtpVerification`/`OtpInput`/`schemas/AuthSchema.ts` were **deleted**; `AuthStore.verifyPhone` → `verifyOtp` + `enterWithTokens`, reset wrappers added; `AuthApi` gains the three reset methods. **NB:** the live `RegisterRequest` in `openapi.json` names the org field `tenantName` while the app model/code use `organizationName` (pre-existing mismatch — kept as-is, not introduced here; flag if register breaks).
- **Not yet built:** Reports.
- **Frontend bug-fix pass (findings.md, 2026-06-24, branch `redesign/bug-fixes`):** addressed the ~28 frontend items from the deployed-env manual sweep. Cross-cutting: **save buttons stay enabled across all modal forms** — `FormDialogFooter`/`SaveButton` validate on submit (rule 5) and are disabled only while saving or while the backend is unreachable; a **backend-outage system** (`ConnectivityStore` + `ConnectivityBridge`, fed by a new `httpErrorInterceptor`) toasts on network/5xx, shows a global `OfflineBanner` in `AppLayout`, and blocks submit until recovery; the http error interceptor **reports handled 4xx/5xx to Sentry** (5xx/network as errors, other 4xx as warnings, 401s skipped); a shared **`MoneyField`** formats amounts with thousands separators as you type (product prices, wallet opening/transfer, payment, payroll) and a shared **`UzsUnit`** unifies the «UZS» suffix spacing; the four hand-rolled tables (Employees/Payments/Wallets/Debt) share **`components/shared/Table/tableStyles.ts`**; a **`NotFoundPage`** catch-all route replaces the blank 404; phone normalization helpers (`uzNationalPart`/`uzPhoneToStored` in `phoneUtils`) give partner + invite the +998/E.164 affordance. Module deltas: **Products** drop `retailPrice` from the create/edit contract (kept read-only on the served model; **create stays blocked until the backend drops the `0<supply<retail<sale` rule**) and align the measurement enum to the backend (`Gram/Kilogram/Ton/Piece/Box/Unit/None`, no `Liter`); **Orders/New Order** drop the `«Нет»`/`None` source (enum is `Telegram`/`OmborWeb`, New Order defaults to `OmborWeb`); the **transaction detail** loads the rich record via `TransactionApi.getById` (warehouse name, payments, number) while still using the list for refund relationships; **refund** create sends `PartnerId`; **opening-stock** sends `warehouseId`; **partner create** re-reads the full partner (lean `CreatePartnerResponse` has no balance); **Settings** enforces the 2 MB logo limit and maps active users to «Активен»; the **Dashboard** keeps its header mounted during a period re-fetch and drops the three «Все …» links. Owner decisions: uz-Latn **gated out** (not backfilled); mobile responsiveness **deferred** (desktop-first). Backend-only items (F-013/027/029) and the backend halves of F-006/017/023 are a separate session.
- **i18n state:** i18next + react-i18next (migrated June 2026, init in `src/i18n/config.ts` — flat keys with literal dots, single merged namespace, ru fallback, locale persisted in localStorage `ombor.locale`). `ru` partially complete; `uz` partial and legacy-named (`supplier`, `supply` namespaces to be consolidated as modules are rewritten); uz-Cyrl registered but empty. **Both Uzbek locales are gated out of the language picker** (`i18n/languages.ts` lists ru only) until uz-Latn (~14% translated) is backfilled — the locales stay registered so persisted prefs resolve. uz-Latn / uz-Cyrl backfill pending.
- **Backend:** does not yet satisfy the redesigned UI — check `tech-change-list.md`, mock per `docs/mocking.md`.
